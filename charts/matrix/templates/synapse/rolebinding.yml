{{- if .Values.synapse.role.create }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "matrix.synapse.roleName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "matrix.synapse.labels" . | nindent 4 }}
  {{- with .Values.synapse.role.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
subjects:
  - kind: ServiceAccount
    namespace: {{ .Release.Namespace }}
    name: {{ include "matrix.synapse.serviceAccountName" . }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "matrix.synapse.roleName" . }}
{{- end }}
