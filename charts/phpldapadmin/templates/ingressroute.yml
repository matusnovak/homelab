apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: {{ printf "%s-ingress" (include "phpldapadmin.fullname" .) }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "phpldapadmin.labels" . | nindent 4 }}
spec:
  routes: 
    - kind: Rule
      match: {{ printf "Host(`%s.%s`)" .Values.subdomain .Values.global.domainName }}
      services:
        - name: {{ include "phpldapadmin.fullname" .}}
          port: 80
  {{- if .Values.global.certResolver }}
  tls:
    certResolver: {{ .Values.global.certResolver }}
  {{- end }}
