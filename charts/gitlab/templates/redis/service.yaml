apiVersion: v1
kind: Service
metadata:
  name: {{ include "gitlab.redis.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gitlab.redis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.redis.service.type }}
  ports:
    - port: 6379
      protocol: TCP
      name: redis
  selector:
    {{- include "gitlab.redis.selectorLabels" . | nindent 4 }}
