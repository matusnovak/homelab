{{- if .Values.cache.role.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "gitlab.cache.roleName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gitlab.cache.labels" . | nindent 4 }}
  {{- with .Values.cache.role.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
rules:
  - apiGroups:
      - 'extensions'
    resources:
      - 'podsecuritypolicies'
    verbs:
      - 'use'
    resourceNames:
      - {{ include "gitlab.cache.fullname" . }}
{{- end }}
